apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: infisical
  namespace: infisical
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: Host(`infisical.bnei.dev`)
    services:
    - name: infisical
      port: 8080
  tls:
    certResolver: le
